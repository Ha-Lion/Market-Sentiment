ChatGPT




Tue, May 26 at 10:54 PM

PublicSentimentDash_next_chat_handoff_2026-05-27(1).txt
Document
go on with fixing. if you need any file, let me know


news_cache(2).json
File

scraper(4).py
Python

status(2).json
File

0925eeda-0190-4793-b4af-315aea65642a.png
8ec8508d-6dd2-4264-9a85-98f32d3abb1a.png
here is the list of all ofthe files i have in github . briefly and short ly let me know what you need


dashboard(10).html
File

index(15).html
File

news-articles(7).html
File

sentiment-history(7).html
File

sentiment-core.js
JavaScript

status(3).json
File

dashboard_data(2).json
File

sentiment_history(5).json
File

instrument_history.json
File

technical_data(1).json
File

is this the run you are talking about?

5662db71-e2ea-43c4-95ff-4223e3ad8cdd.png
8cc22ea6-3ae3-4310-8809-4bfa2c43eb2d.png

status(4).json
File

dashboard_data(3).json
File


should I just past all four info like this?

ther eis a token value for cloudflare API. what should i name it in github and save it as?



scrape-and-publish(5).yml
File

Please don't include DEV, version, this kind of wording in the chart . i want to see as if it's real
the chart is too small and the selection button on the left is great idea, but too big. I would suggest to make them smallerand stack up top 15 or 20 and make the chart bigger. see the picture first

i am getting this notification any time i switch instrument.  when I ex it, i have instrument on the chart. fix it. 
make the left side selection buttons smaller, not bold font, and stretch the chart side way.

add majour currency pairs too. and organize the instruments in a logical way.
I still get this error.
make the header if the card that is holding the chart smaller and make the fonts smaller too.

I need you to make the header of the card that is holding the chart smaller and move the description from below the instrument's name to its right side.
ther eis a room to add two more instruments to any section. add two more that you think people would use the most.
also, change the name from MJOR FOREX to FOREX and remove US from Index ETFS

for now, I don't want to add it as an official page. I wantto add it as a feature to my interactive dashboard. 
in these five cards, ther eis a button for Deep Analysis. I want to make it smaller and add another button next to it and lable it Chart. depending on the chosen instrument in any particular box, the chart will open that particular instrument. yo can  the chart will popup as it is now exactly. only should have a x botton to close it and go back to interactive dashboard page. 


dashboard(11).html
File


dashboard-lab-v9-button-only-more-instruments(1).txt
Document
i don't know. just give me the full code txt file with this version



bitcoin-sentiment-today.html
File

site(14).css
File

dashboard_data(4).json
File

instrument_history(1).json
File

sentiment-core(1).js
JavaScript

news and articles are over the logo and you need to stretch the news cards in width and add more details to it . see the pic

can you see the picture? what is wrong in this pic?


scraper(5).py
Python
other two files you have the latest


dashboard(12).html
File

sitemap(6).xml
File

it's just showing some gibberish. see the picture, roll back and redo it. don't do patch work

general precious metal & inflation / real yield title on its card is not complete. baybe you wrap the title or make the font smaller without hurting the rest ofthe card's info.


scraper(6).py
Python
let's do step 2. make sure you review the wole coding first and then add updates to it.

DAX seems out of line


dashboard(13).html
File

index(16).html
File

market-sentiment(8).html
File

news-articles(8).html
File

sentiment-history(8).html
File

advertise(3).html
File

about(8).html
File

contact(8).html
File
here the files you asked. Please make sure you DO not change anything or mess up anything in these pages. just add new pages to the ribbon and place them in a new row, below under the original row of pages, with tiny relative icon and the same format and style.

icons are missing from next to the page titles. see the pic


pdf-report(1).js
JavaScript

vote-widget(7).js
JavaScript

Tue, Jun 16 at 5:49 PM
why i have already used 90% of my credits? i supposed to have it for the whole month? didn't we set it up that way?

i requested a detailed report and it will come via email soon and i will share with you. but so far, this is the screenshot


a6328874-4293-4b0c-8a8e-bc9a11762b99.png
1130f486-91d3-4e8e-a86a-517bc93f800b.png

i did click on action and disabled it



769a4668-a38f-41a5-b734-400a31741a34.yml
File



Mon, Jul 6 at 12:36 PM
almost every day or so, this will fail once mostly in the morning. how can we findout about the reason and eventually fix it?

is this what you wanted to see?

here is more details

Thu, Jul 9 at 8:19 AM
it happened again. this job failed

please help me step by step. I want to raise $10,000



0066362e-a44e-4820-9b2a-5d4d301774d0.png
acada511-d798-4f19-b272-8752d72f17ea.png
7242dfd5-c430-4097-9016-d9678b322bf2.png

i already have a banner. we can update it to something better.
it says Partner with public sentiment dash .......
see the picture.

i don't see search box.



38f022f9-c75e-4925-a126-08cb0bf2a1c4.js
JavaScript
please see the file, update the code properly, and sent it back to me as txt file. i don't like to do surgery like this.

in some pages are changed and in some are not.
here are the lists.

Crypto Sentiment is completly missing the banner
Energy Sentiment, Precious Metals and Indices are having the updated banner
all 5 linkes up there are having the old banner.


crypto.html
File
see the attached and surgically fix it. don't change anything else

2c933ab9-8c9f-4d70-acf1-83164c96cbfd.png
62f4e065-3901-4aad-bb48-e784bd14f90e.png
NO. it doesn't look like other banners. why?  it also has double icon on each page on the ribbon


charts.html
File
please see the attached and tell me if that is the right page and it's good to go

Ribbon is missing.  i need to have a normal ribbon like other pages.

i forgot to tell you that in the ribbon, I have Crypto Sentiment, Energy Sentiment but for precious metal and indices ther eis no word sentiment after.
Also, i would like to shrink the ribbon just a little bit and reduce the width. right now, it seems ther are three rows of links to different pages. can you make it to two rows?
please update the code for all of the updates for ribbon and send me the txt file

not good at all. but let's fix a couple of things and then try the video again.
there are two issues in the ribbon.
the support / donation widget , the message is duplicated. remove the "_support the free project " andupdate the oval box that said "Support Project".
Also, Constantly Learning & Improving widget has a small oval box. it needs to be resized and stretch the width. see the pic.



this is a screenshot from Interactive Dashboard page. the skyline chart has references on the right side.please make the fonts smaller that they fit into the outlines and just stretch the outlines and make them fram little bit bigger by stretching them more to the right. don't resize the chart itself. before anything, i need you to tell me which file belongs to this page that i can share with you the latest version


dashboard(14).html
File

3ea6a305-ed93-42ba-8e40-bac60a687cbf.png
fa2086d5-821b-4485-af7c-8da3842bbb29.png
i just noticed another issue. see the pics. monthly and daily, shows the same chart. on monthly, i should have 6 to 12 months. Don't I have such back up data?


instrument_history(2).json
File

sentiment-core(2).js
JavaScript


scraper(7).py
Python

ok.  just to make sure we don't show the name of the months on the page or any indication ofthe total duration (6 month, 12 month .....).
also, please see the attached pic. there is a note in gray that talks about 6 months view !! but the heatwave shows past 12 hours !! I am not sure what is goung on and how this heatwave works. briefly explain

also, on the bottom of skyline, ther eis a note and list of instruments where i have on the righside of the chart too. Please remove it.

NOW;
please itemize very briefly all of the actions and changes that we need to do to this particular page. very breifly. i want to make sure we don't miss anything

skyline shows a streghit line for monthly. is it because of lack of data that we haven't accumulated yet?

what shoould we do with the gap below skyline chart?

then the strenght ranking will look odd 


sentiment-history(9).html
File
brifly and in short itemize your finding in this file.



scraper(8).py
Python
please see the attached and update it and send me txt file

I need you to do a couple of more things.
1- the function / button Export CSV, should export past 6 months hourly data.
2- move up all of the buttons on the left saide and level the height with Hourly Sentiment Pulse graph.
3- move Monthly Global bias mix all the way to the left of the box right below buttons.
4- Stretch MOnthly Global vs U.S divergence graph tothe left as you make more room by changes that i mentioned in number 2 and 3.
any question?


sentiment-history (1)(1).html
File

you tell me what is wrong with your changes 

one tiny change to interactive dashboaRrd.
see the picture. there are three buttons Daily, Weekly and Monthly right above skyline which will affect the data presentation in every graph in this page. but it's placed in skyline graph. i ned you to move it up inside the title box on the bottom-right corner and make them slighty and tiny bigger.


news-articles(9).html
File

i have top 10. should we add another 5? what do you think?


crypto(1).html
File


energy.html
File


precious-metals.html
File


indices.html
File

i applied the code to dasdhboard-lab page to fix and update it and then the final version will be launched. i think the line that you have under the title is wrong. pleaqe read it

please change the note in the title to be general and explains that here we have assets that are moving based on polocies and geopolitical challenges in the world. I forgot to mention that geopolotical is another important fource in the market. I don't know if ther eshould be a seperate page for geopolitical or we can add/remove assets here to include that category as well. what do you think? what else we can add? briefly and short

you don't have all of the pages.
2962e471-a1e9-4f61-9134-5b9409da1cc3.png
plus home page

ec7bd370-3f7d-4623-818d-703862017aa6.png
i don't know how to download

84c528fb-04d7-4692-bf8a-6384f8259a59.png
it needs to be fixed.

1651138c-8af6-468e-b3e9-42c1b044366b.png
go back tothe previous version and ONLY move down the donation widgit  and nothing else

ad54200c-b7cc-47e2-b2a9-ba369b777782.png
you moved it in the middle of page. go back to version 1, and only move the donation little bit down NOT TOOOOOOOO MUCH

8a2bd4e9-0c62-4b80-b139-2457aabb57db.png
you completly deleted the ribbon.
again, i repeat for the 10th time. go back to version 1.
push the the donation box little bit down. again, from version 1. the only problem with version one was that donation was on the link tothe pages. it just need a tiny move. no changing the shape and size or anything else.

Worked for 1m 0s
Yes, I comprehend. The mistake was trying to fix this through site-nav.js.

Correct fix:

Restore site-nav.js back to version 1 exactly

Move the donation banner from the vote widget file only

Change only this:

bottom:16px;
to:

bottom:11px;
That is a tiny move down — nothing else.

Upload these two files:

site-nav-v5-exact-v1-restore.txt → upload as site-nav.js

vote-widget-v24-donation-tiny-down.txt → upload as vote-widget.js

Syntax check passed on both.


why i need to change the vote widgit?

You do not need to change the vote widget unless the donation banner is actually coming from vote-widget.js.

From your screenshot, the donation box looks like a floating donation widget, not part of the ribbon itself. That is why I suggested vote-widget.js — because changing site-nav.js alone affected the ribbon/layout instead of only the donation box.

But you are right to question it. The safe path is:

Restore site-nav.js to version 1 exactly.

Check which file creates the donation banner:

If donation box comes from vote-widget.js, then change only vote-widget.js.

If donation box comes from another file, we should change that file instead.

If it is inside each HTML page, then we change only that small CSS line there.

So for now, upload only:

site-nav-v5-exact-v1-restore.txt → site-nav.js
Do not upload the vote-widget file yet.

Then send me or upload your current vote-widget.js, and I’ll confirm exactly where the donation box is coming from before changing anything else.


i belive you are right. donation widget is coming from vote widget.

Yes, then use the two-file fix:

Upload this as:

site-nav.js
site-nav-v5-exact-v1-restore.txt

Upload this as:

vote-widget.js
vote-widget-v24-donation-tiny-down.txt

This should do exactly what you wanted:

restore ribbon back to version 1

keep ribbon shape/size/style the same

move only the donation box slightly down

no page layout changes

After upload, refresh with:

Ctrl + F5
If it still looks wrong, stop there and send me the screenshot before changing more files.


so, it has to be removed from vote and added to the ribbon

Worked for 1m 31s
Yes — exactly. The donation banner should not be created by vote-widget.js anymore. It should be part of the shared ribbon/header in site-nav.js.

Upload these two files:

site-nav-v6-donation-in-ribbon.txt
Upload as:

site-nav.js
vote-widget-v25-remove-donation-banner.txt
Upload as:

vote-widget.js
What changed:

site-nav.js now controls the donation banner inside the ribbon.

vote-widget.js no longer creates the floating donation banner.

This should prevent the donation box from floating into the middle of the page or overlapping strangely.

Syntax check passed on both.


vote-widget-v25-remove-donation-banner.txt


const PSD_SUPABASE_URL = "https://fupexuonvzakoguucglk.supabase.co";
const PSD_SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZ1cGV4dW9udnpha29ndXVjZ2xrIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzc5MDUzNTQsImV4cCI6MjA5MzQ4MTM1NH0.YZF4SBqvDTSOyHDOf_TVhpBXDm0FEma74u32Bdryfjg";
const PSD_GA4_ID = "G-BZMQQZ2SVC";
const PSD_SITE_URL = "https://publicsentimentdash.com";
// v25: donation banner moved to site-nav.js; vote widget no longer creates it
const PSD_X_PROFILE_URL = "https://x.com/PublicSentDash";

const PSD_VOTE_INSTRUMENTS = [
  "S&P 500 / ES","Nasdaq / NQ","Dow / YM","Russell / RTY","VIX",
  "DAX","FTSE 100","Nikkei 225","Hang Seng","Euro Stoxx 50","CAC 40",
  "US 2Y Treasury","US 10Y Treasury","Treasury Yields",
  "US Dollar / DXY","EUR / EURUSD","GBP / GBPUSD","JPY / USDJPY","CHF / USDCHF",
  "CAD / USDCAD","AUD / AUDUSD","NZD / NZDUSD","EURJPY","EURGBP","GBPJPY",
  "AUDJPY","CADJPY","EURCHF","EURCAD","AUDCAD","AUDNZD","NZDJPY",
  "USDTRY","USDMXN","USDZAR",
  "Bitcoin / BTC","Ethereum / ETH","Solana / SOL","XRP","BNB","Cardano / ADA",
  "Dogecoin / DOGE","General Crypto",
  "Gold","Silver","Copper","Crude Oil","Natural Gas",
  "Crypto Regulation",
  "Crypto ETFs / Flows",
  "General Energy",
  "OPEC / Supply",
  "Oil Inventories / EIA",
  "Brent Oil",
  "WTI Oil",
  "Gasoline",
  "Heating Oil",
  "General Precious Metals",
  "Platinum",
  "Palladium",
  "Gold ETFs / Flows",
  "Central Bank Gold",
  "Mining Stocks",
  "Inflation / Real Yields",
  "Fed / FOMC","CPI / Inflation","PPI","Jobs / NFP","US GDP / Growth","Geopolitical / Tariffs"
];

window.PSD_USER_SENTIMENT = window.PSD_USER_SENTIMENT || {};

function psdLoadGA4(){
  if(!PSD_GA4_ID || window.PSD_GA4_LOADED) return;
  window.PSD_GA4_LOADED = true;
  window.dataLayer = window.dataLayer || [];
  window.gtag = function(){ window.dataLayer.push(arguments); };

  const script = document.createElement("script");
  script.async = true;
  script.src = "https://www.googletagmanager.com/gtag/js?id=" + encodeURIComponent(PSD_GA4_ID);
  document.head.appendChild(script);

  window.gtag("js", new Date());
  window.gtag("config", PSD_GA4_ID, {
    page_title: document.title,
    page_path: window.location.pathname
  });
}

function psdTrack(eventName, params){
  if(typeof window.gtag === "function"){
    window.gtag("event", eventName, params || {});
  }
}

function psdCanonicalURL(){
  const canonical = document.querySelector('link[rel="canonical"]');
  if(canonical && canonical.href) return canonical.href;
  const path = window.location.pathname === "/" ? "/" : window.location.pathname;
  return PSD_SITE_URL + path;
}

function psdMetaDescription(){
  const meta = document.querySelector('meta[name="description"]');
  return meta ? meta.getAttribute("content") || "" : "";
}

function psdInjectStructuredData(){
  if(document.getElementById("psdStructuredData")) return;

  const currentUrl = psdCanonicalURL();
  const description = psdMetaDescription();
  const title = document.title || "Public Sentiment Dash";

  const schema = {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "Organization",
        "@id": PSD_SITE_URL + "/#organization",
        "name": "Public Sentiment Dash",
        "url": PSD_SITE_URL + "/",
        "logo": PSD_SITE_URL + "/logo.png",
        "sameAs": [PSD_X_PROFILE_URL],
        "description": "AI-assisted public market sentiment dashboard for stocks, forex, crypto, commodities, bonds, macro headlines, and financial news."
      },
      {
        "@type": "WebSite",
        "@id": PSD_SITE_URL + "/#website",
        "url": PSD_SITE_URL + "/",
        "name": "Public Sentiment Dash",
        "publisher": {"@id": PSD_SITE_URL + "/#organization"},
        "inLanguage": "en-US"
      },
      {
        "@type": "WebPage",
        "@id": currentUrl + "#webpage",
        "url": currentUrl,
        "name": title,
        "description": description,
        "isPartOf": {"@id": PSD_SITE_URL + "/#website"},
        "publisher": {"@id": PSD_SITE_URL + "/#organization"},
        "inLanguage": "en-US"
      }
    ]
  };

  const script = document.createElement("script");
  script.id = "psdStructuredData";
  script.type = "application/ld+json";
  script.textContent = JSON.stringify(schema);
  document.head.appendChild(script);
}

function psdEscape(value){
  return String(value ?? "")
    .replace(/&/g,"&amp;")
    .replace(/</g,"&lt;")
    .replace(/>/g,"&gt;")
    .replace(/"/g,"&quot;")
    .replace(/'/g,"&#039;");
}

function psdClass(value){
  const clean = String(value || "N/A").replace(/[^a-zA-Z]/g,"");
  return clean || "NA";
}

function psdCreateAdvertiseBanner(){
  // Donation banner is now rendered by site-nav.js as part of the shared ribbon.
  // Keep this function as a no-op so older calls do not create a duplicate floating banner.
}
function psdGetVoterId(){
  let id = localStorage.getItem("psd_voter_id");
  if(!id){
    id = window.crypto && crypto.randomUUID
      ? crypto.randomUUID()
      : "voter_" + Math.random().toString(36).slice(2) + Date.now().toString(36);
    localStorage.setItem("psd_voter_id", id);
  }
  return id;
}

function psdHeaders(){
  return {
    "apikey": PSD_SUPABASE_ANON_KEY,
    "Authorization": "Bearer " + PSD_SUPABASE_ANON_KEY,
    "Content-Type": "application/json"
  };
}


function psdApplyCompactRibbonLayout(){
  if(document.getElementById("psdCompactRibbonCss")) return;

  const style = document.createElement("style");
  style.id = "psdCompactRibbonCss";
  style.textContent = `
    .header{
      padding:8px 22px 7px !important;
      gap:14px !important;
      min-height:148px !important;
    }

    .logo{
      width:112px !important;
      height:112px !important;
      padding:6px !important;
    }

    .brand{
      gap:11px !important;
    }

    .brand-stamp,
    .page-chip{
      padding:7px 12px !important;
      font-size:12px !important;
    }

    .header-pill{
      min-width:340px !important;
      padding:8px 30px !important;
      font-size:12px !important;
      justify-content:center !important;
      text-align:center !important;
      white-space:nowrap !important;
    }

    .site-subtitle{
      font-size:12px !important;
    }

    .header-center{
      min-width:360px !important;
      transform:translateY(-5px) !important;
    }

    .nav{
      gap:6px 8px !important;
      transform:translateY(-5px) !important;
      align-content:flex-start !important;
    }

    .nav a{
      font-size:11.5px !important;
      line-height:1.02 !important;
      padding:5px 6px !important;
      gap:4px !important;
      white-space:nowrap !important;
    }

    .nav a::before{
      width:12px !important;
      min-width:12px !important;
      font-size:10.5px !important;
    }

    .social-links{
      gap:6px !important;
      margin-left:6px !important;
      margin-top:0 !important;
    }

    .social-label{
      font-size:11px !important;
    }

    .social-pill{
      min-width:38px !important;
      padding:6px 10px !important;
      font-size:11px !important;
    }

    .social-pill.linkedin{
      min-width:68px !important;
    }

    @media(max-width:1180px){
      .header{
        min-height:auto !important;
      }
      .header-center,
      .nav{
        transform:none !important;
      }
    }
  `;
  document.head.appendChild(style);
}

function psdNormalizeTopNavIcons(){
  const navLinkMap = {
    "dashboard.html": { label:"Interactive Dashboard", icon:"📊" },
    "charts.html": { label:"Market Charts", icon:"📈" },
    "sentiment-history.html": { label:"Historical Sentiment", icon:"📈" },
    "news-articles.html": { label:"News & Articles", icon:"📰" },
    "market-sentiment.html": { label:"Guides", icon:"📘" },
    "advertise.html": { label:"Business Opportunities", icon:"💼" },
    "contact.html": { label:"Get in Touch", icon:"✉️" },
    "about.html": { label:"About", icon:"ℹ️" },
    "crypto.html": { label:"Crypto Sentiment", icon:"🪙" },
    "energy.html": { label:"Energy Sentiment", icon:"⚡" },
    "precious-metals.html": { label:"Precious Metals Sentiment", icon:"🥇" },
    "indices.html": { label:"Indices Sentiment", icon:"📊" }
  };

  if(!document.getElementById("psdRibbonIconCss")){
    const style = document.createElement("style");
    style.id = "psdRibbonIconCss";
    style.textContent = Object.entries(navLinkMap).map(([href, item]) =>
      `.nav a[href="${href}"]::before{content:"${item.icon}"!important;}`
    ).join("\n");
    document.head.appendChild(style);
  }

  document.querySelectorAll(".nav a[href]").forEach(link => {
    const href = (link.getAttribute("href") || "").split("#")[0].split("?")[0].toLowerCase();
    const key = href.split("/").pop();
    const item = navLinkMap[key];
    if(!item) return;

    link.href = key;
    link.textContent = item.label;
  });
}

function psdEnsureMarketChartsNavLink(){
  const nav = document.querySelector(".nav");
  if(!nav) return;

  const currentPath = window.location.pathname.toLowerCase();
  const isChartsPage = currentPath.endsWith("/charts.html") || currentPath.endsWith("charts.html");

  let chartsLink = Array.from(nav.querySelectorAll("a[href]")).find(link => {
    const href = (link.getAttribute("href") || "").split("#")[0].split("?")[0].toLowerCase();
    return href === "charts.html" || href.endsWith("/charts.html");
  });

  if(!chartsLink){
    chartsLink = document.createElement("a");
    chartsLink.href = "charts.html";

    const dashboardLink = Array.from(nav.querySelectorAll("a[href]")).find(link => {
      const href = (link.getAttribute("href") || "").split("#")[0].split("?")[0].toLowerCase();
      return href === "dashboard.html" || href.endsWith("/dashboard.html");
    });

    const rowBreak = nav.querySelector(".nav-row-break");

    if(dashboardLink){
      dashboardLink.insertAdjacentElement("afterend", chartsLink);
    }else if(rowBreak){
      nav.insertBefore(chartsLink, rowBreak);
    }else{
      nav.insertBefore(chartsLink, nav.firstChild);
    }
  }

  chartsLink.href = "charts.html";
  chartsLink.textContent = "Market Charts";
  chartsLink.classList.toggle("active", isChartsPage);
}

function psdEnhanceFooterLegalLinks(){
  document.querySelectorAll(".footer-links").forEach(footer => {
    const extraLinks = [
      ["advertise.html", "Business Opportunities"],
      ["privacy.html", "Privacy"],
      ["terms.html", "Terms"],
      ["disclaimer.html", "Disclaimer"]
    ];

    extraLinks.forEach(([href, label]) => {
      const exists = Array.from(footer.querySelectorAll("a")).some(a => (a.getAttribute("href") || "") === href);
      if(!exists){
        const a = document.createElement("a");
        a.href = href;
        a.textContent = label;
        if(window.location.pathname.toLowerCase().endsWith("/" + href.toLowerCase())){
          a.className = "active";
        }
        footer.appendChild(a);
      }
    });
  });
}

function psdEnhanceSocialLinks(){
  document.querySelectorAll(".social-links").forEach(social => {
    const xPill = Array.from(social.querySelectorAll(".social-pill")).find(el =>
      el.textContent.trim().toLowerCase() === "x"
    );

    if(xPill && xPill.tagName.toLowerCase() !== "a"){
      const a = document.createElement("a");
      a.className = xPill.className;
      a.textContent = "X";
      a.href = PSD_X_PROFILE_URL;
      a.target = "_blank";
      a.rel = "noopener";
      xPill.replaceWith(a);
    }else if(xPill){
      xPill.href = PSD_X_PROFILE_URL;
      xPill.target = "_blank";
      xPill.rel = "noopener";
    }
  });
}

function psdFallbackFromElement(el){
  const card = el.closest(".instrument-card");
  if(card){
    const dailyRow = Array.from(card.querySelectorAll(".info-row")).find(row =>
      row.textContent.trim().toLowerCase().startsWith("daily:")
    );
    if(dailyRow){
      const text = dailyRow.textContent.toLowerCase();
      if(text.includes("bullish")) return "Bullish";
      if(text.includes("bearish")) return "Bearish";
      if(text.includes("neutral")) return "Neutral";
    }
  }

  const rail = el.closest(".rail-item");
  if(rail){
    if(rail.classList.contains("direction-up")) return "Bullish";
    if(rail.classList.contains("direction-down")) return "Bearish";
    if(rail.classList.contains("direction-neutral")) return "Neutral";
  }

  const newsCard = el.closest(".news-card");
  if(newsCard){
    const tech = newsCard.querySelector(".tech-chip");
    const text = tech ? tech.textContent.toLowerCase() : "";
    if(text.includes("bullish")) return "Bullish";
    if(text.includes("bearish")) return "Bearish";
    if(text.includes("neutral")) return "Neutral";
  }

  return "N/A";
}

function psdEffectiveSentiment(instrument, fallback){
  const voted = window.PSD_USER_SENTIMENT?.[instrument];
  if(voted && voted !== "N/A") return voted;
  return fallback || "N/A";
}

async function psdLoadUserSentiment(){
  if(!PSD_SUPABASE_URL || !PSD_SUPABASE_ANON_KEY) return;

  try{
    const response = await fetch(`${PSD_SUPABASE_URL}/rest/v1/rpc/get_user_sentiment`, {
      method:"POST",
      headers:psdHeaders(),
      body:"{}"
    });

    if(!response.ok){
      console.warn("User sentiment load failed:", response.status);
      return;
    }

    const rows = await response.json();
    const map = {};
    rows.forEach(row => {
      map[row.instrument] = row.user_sentiment || "N/A";
    });

    window.PSD_USER_SENTIMENT = map;
    psdApplyUserSentiment();
    window.dispatchEvent(new CustomEvent("psdUserSentimentReady", { detail: map }));
  }catch(error){
    console.warn("User sentiment load failed", error);
  }
}

function psdApplyUserSentiment(){
  document.querySelectorAll("[data-user-sentiment]").forEach(el => {
    const instrument = el.getAttribute("data-user-sentiment");
    const fallback = psdFallbackFromElement(el);
    const value = psdEffectiveSentiment(instrument, fallback);

    if(el.classList.contains("user-chip")){
      el.textContent = "User: " + value;
      el.className = "chip user-chip " + psdClass(value);
    }else{
      el.textContent = value;
      el.className = "psd-user-sentiment-value " + psdClass(value);
    }
  });
}

async function psdSubmitVote(instrument, vote){
  const status = document.getElementById("psdVoteStatus");
  if(!status) return;

  status.textContent = "Submitting...";
  status.className = "psd-vote-status";

  try{
    const response = await fetch(`${PSD_SUPABASE_URL}/rest/v1/rpc/submit_instrument_vote`, {
      method:"POST",
      headers:psdHeaders(),
      body:JSON.stringify({
        p_instrument: instrument,
        p_vote: vote,
        p_voter_id: psdGetVoterId()
      })
    });

    let result = {};
    try{ result = await response.json(); }catch(e){ result = {}; }

    if(!response.ok || !result.ok){
      status.textContent = result.error || `Vote failed. Error ${response.status}`;
      status.className = "psd-vote-status error";
      console.warn("Vote failed:", response.status, result);
      return;
    }

    window.PSD_USER_SENTIMENT[instrument] = result.user_sentiment || "N/A";
    psdApplyUserSentiment();

    psdTrack("instrument_vote", {
      instrument: instrument,
      vote: vote,
      result_sentiment: result.user_sentiment || "N/A"
    });

    status.textContent = "Vote saved.";
    status.className = "psd-vote-status success";

    setTimeout(() => {
      const panel = document.getElementById("psdVotePanel");
      if(panel) panel.classList.remove("open");
    }, 700);
  }catch(error){
    status.textContent = "Vote failed.";
    status.className = "psd-vote-status error";
    console.warn("Vote failed:", error);
  }
}

function psdCreateVoteWidget(){
  if(document.getElementById("psdVoteWidget")) return;

  if(!document.getElementById("psdVoteWidgetFallbackCss")){
    const style = document.createElement("style");
    style.id = "psdVoteWidgetFallbackCss";
    style.textContent = `
      .psd-vote-widget{position:fixed;left:18px;top:50%;transform:translateY(-50%);z-index:1000;font-family:Inter,Segoe UI,Arial,sans-serif}
      .psd-vote-tab{display:flex;flex-direction:column;align-items:center;gap:4px;width:54px;min-height:78px;border:1px solid rgba(210,153,34,.45);border-radius:18px;background:linear-gradient(180deg,rgba(210,153,34,.22),rgba(13,17,23,.96));color:#ffd780;cursor:pointer;box-shadow:0 0 24px rgba(210,153,34,.18)}
      .psd-vote-tab-icon{font-size:20px;line-height:1}.psd-vote-tab-text{font-size:12px;font-weight:700}
      .psd-vote-panel{position:absolute;left:66px;top:50%;transform:translateY(-50%);width:290px;display:none;padding:16px;border-radius:18px;border:1px solid #263241;background:rgba(13,17,23,.98);box-shadow:0 20px 60px rgba(0,0,0,.42)}
      .psd-vote-panel.open{display:block}.psd-vote-title{color:#fff;font-size:17px;font-weight:700;margin-bottom:4px}.psd-vote-note,.psd-vote-status{color:#8b949e;font-size:12px}.psd-vote-label{display:block;color:#c9d1d9;font-size:12px;font-weight:600;margin:10px 0 6px}
      .psd-vote-select{width:100%;background:#111821;color:#e6edf3;border:1px solid #263241;border-radius:12px;padding:10px 12px}.psd-vote-actions{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:12px 0}.psd-vote-choice{border:1px solid #263241;border-radius:12px;padding:10px;color:#fff;background:rgba(17,24,33,.88);cursor:pointer;font-weight:600}
      .psd-vote-choice.bullish.active{border-color:rgba(63,185,80,.65);background:rgba(63,185,80,.16);color:#9ff0aa}.psd-vote-choice.bearish.active{border-color:rgba(248,81,73,.65);background:rgba(248,81,73,.16);color:#ffaaa6}
      .psd-vote-submit{width:100%;border:0;border-radius:999px;padding:11px 14px;background:#d29922;color:#05070b;cursor:pointer;font-weight:800}.psd-vote-cancel{width:100%;margin-top:8px;border:1px solid #263241;border-radius:999px;padding:10px 14px;background:transparent;color:#c9d1d9;cursor:pointer;font-weight:600}
    `;
    document.head.appendChild(style);
  }

  const wrap = document.createElement("div");
  wrap.id = "psdVoteWidget";
  wrap.className = "psd-vote-widget";

  wrap.innerHTML = `
    <button class="psd-vote-tab" type="button" aria-label="Open voting widget">
      <span class="psd-vote-tab-icon">↕</span>
      <span class="psd-vote-tab-text">Vote<br>Here</span>
    </button>

    <div class="psd-vote-panel" id="psdVotePanel">
      <div class="psd-vote-title">Your Market Vote</div>
      <div class="psd-vote-note">Anonymous daily vote. No registration.</div>

      <label class="psd-vote-label" for="psdVoteInstrument">Instrument</label>
      <select id="psdVoteInstrument" class="psd-vote-select">
        ${PSD_VOTE_INSTRUMENTS.map(x => `<option value="${psdEscape(x)}">${psdEscape(x)}</option>`).join("")}
      </select>

      <div class="psd-vote-actions">
        <button type="button" class="psd-vote-choice bullish active" data-vote="Bullish">Bullish</button>
        <button type="button" class="psd-vote-choice bearish" data-vote="Bearish">Bearish</button>
      </div>

      <button type="button" class="psd-vote-submit" id="psdVoteSubmit">Submit Vote</button>
      <button type="button" class="psd-vote-cancel" id="psdVoteCancel">Cancel</button>

      <div class="psd-vote-status" id="psdVoteStatus"></div>
    </div>
  `;

  document.body.appendChild(wrap);

  const tab = wrap.querySelector(".psd-vote-tab");
  const panel = wrap.querySelector("#psdVotePanel");
  const submit = wrap.querySelector("#psdVoteSubmit");
  const cancel = wrap.querySelector("#psdVoteCancel");
  const choices = wrap.querySelectorAll(".psd-vote-choice");

  let selectedVote = "Bullish";

  tab.addEventListener("click", () => {
    panel.classList.toggle("open");
    psdTrack("vote_widget_toggle", { open: panel.classList.contains("open") });
  });

  cancel.addEventListener("click", () => {
    panel.classList.remove("open");
  });

  choices.forEach(btn => {
    btn.addEventListener("click", () => {
      choices.forEach(x => x.classList.remove("active"));
      btn.classList.add("active");
      selectedVote = btn.getAttribute("data-vote");
    });
  });

  submit.addEventListener("click", () => {
    const instrument = document.getElementById("psdVoteInstrument").value;
    psdSubmitVote(instrument, selectedVote);
  });
}


/*
  PSD PDF Widget
  - Built cleanly on top of the stable vote-widget.js file.
  - Does not touch vote logic, Supabase logic, GA4, structured data, ad banner, footer links, or live update logic.
*/
function psdShouldShowPdfWidget(){
  const path = window.location.pathname.toLowerCase();
  return (
    path === "/" ||
    path.endsWith("/index.html") ||
    path.endsWith("index.html") ||
    path.endsWith("/dashboard.html") ||
    path.endsWith("dashboard.html") ||
    path.endsWith("/sentiment-history.html") ||
    path.endsWith("sentiment-history.html") ||
    path.endsWith("/news-articles.html") ||
    path.endsWith("news-articles.html") ||
    path.endsWith("/crypto.html") ||
    path.endsWith("crypto.html") ||
    path.endsWith("/energy.html") ||
    path.endsWith("energy.html") ||
    path.endsWith("/precious-metals.html") ||
    path.endsWith("precious-metals.html") ||
    path.endsWith("/indices.html") ||
    path.endsWith("indices.html")
  );
}

function psdLoadPdfReportEngine(){
  return new Promise((resolve, reject) => {
    if(typeof window.psdOpenPdfReport === "function"){
      resolve();
      return;
    }

    const existing = document.getElementById("psdPdfReportScript");
    if(existing){
      existing.addEventListener("load", () => resolve(), { once:true });
      existing.addEventListener("error", () => reject(new Error("PDF report engine failed to load.")), { once:true });
      return;
    }

    const script = document.createElement("script");
    script.id = "psdPdfReportScript";
    script.src = "pdf-report.js?v=40";
    script.defer = true;
    script.onload = () => resolve();
    script.onerror = () => reject(new Error("PDF report engine failed to load."));
    document.head.appendChild(script);
  });
}

function psdCreatePdfWidget(){
  if(!psdShouldShowPdfWidget()){
    const existing = document.getElementById("psdPdfWidget");
    if(existing) existing.remove();
    return;
  }

  if(document.getElementById("psdPdfWidget")) return;

  if(!document.getElementById("psdPdfWidgetCss")){
    const style = document.createElement("style");
    style.id = "psdPdfWidgetCss";
    style.textContent = `
      .psd-pdf-widget{position:fixed;left:18px;top:calc(50% + 96px);transform:translateY(-50%);z-index:999;font-family:Arial,Segoe UI,sans-serif}
      .psd-pdf-tab{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:5px;width:54px;min-height:78px;border:1px solid rgba(115,176,255,.72);border-radius:18px;background:#132844;color:#ffffff;cursor:pointer;box-shadow:0 0 14px rgba(88,166,255,.20);animation:psdPdfFloat 3.5s ease-in-out infinite;padding:8px 5px;text-align:center;-webkit-font-smoothing:auto;text-rendering:geometricPrecision}
      .psd-pdf-tab:hover{background:#18365c;transform:translateX(2px)}
      .psd-pdf-tab-icon{font-size:18px;line-height:1;font-weight:800;color:#ffffff;letter-spacing:0}
      .psd-pdf-tab-text{font-size:10.5px;font-weight:700;line-height:1.05;text-align:center;max-width:48px;color:#ffffff;text-shadow:0 1px 1px rgba(0,0,0,.55);letter-spacing:0}
      @keyframes psdPdfFloat{0%,100%{transform:translateY(4px)}50%{transform:translateY(-4px)}}
      @media(max-width:760px){.psd-pdf-widget{left:10px;top:calc(50% + 96px)}.psd-pdf-tab{width:54px;min-height:78px}}
      @media print{#psdVoteWidget,#psdPdfWidget{display:none!important}}
    `;
    document.head.appendChild(style);
  }

  const wrap = document.createElement("div");
  wrap.id = "psdPdfWidget";
  wrap.className = "psd-pdf-widget";
  wrap.innerHTML = `
    <button class="psd-pdf-tab" type="button" aria-label="Save this page as PDF report" title="Save this page as PDF report">
      <span class="psd-pdf-tab-icon">PDF</span>
      <span class="psd-pdf-tab-text">Save<br>PDF</span>
    </button>
  `;

  document.body.appendChild(wrap);

  const btn = wrap.querySelector(".psd-pdf-tab");
  btn.addEventListener("click", async () => {
    const original = btn.innerHTML;
    btn.innerHTML = `<span class="psd-pdf-tab-icon">…</span><span class="psd-pdf-tab-text">Wait<br>PDF</span>`;
    btn.disabled = true;

    try{
      await psdLoadPdfReportEngine();
      if(typeof window.psdOpenPdfReport === "function"){
        psdTrack("pdf_report_open", { page_path: window.location.pathname });
        await window.psdOpenPdfReport();
      }else{
        window.print();
      }
    }catch(error){
      console.warn("PDF report failed", error);
      alert("PDF report failed to load. Please refresh and try again.");
    }finally{
      btn.innerHTML = original;
      btn.disabled = false;
    }
  });
}

function psdSafe(name, fn){
  try{
    return fn();
  }catch(error){
    console.warn("PSD widget helper failed:", name, error);
    return null;
  }
}

function psdInit(){
  psdSafe("create vote widget", psdCreateVoteWidget);
  psdSafe("create PDF widget", psdCreatePdfWidget);
  psdSafe("load GA4", psdLoadGA4);
  psdSafe("inject structured data", psdInjectStructuredData);
  psdSafe("apply compact ribbon layout", psdApplyCompactRibbonLayout);
  psdSafe("create advertise banner", psdCreateAdvertiseBanner);
  psdSafe("ensure market charts nav link", psdEnsureMarketChartsNavLink);
  psdSafe("normalize top nav icons", psdNormalizeTopNavIcons);
  psdSafe("enhance footer legal links", psdEnhanceFooterLegalLinks);
  psdSafe("enhance social links", psdEnhanceSocialLinks);
  psdSafe("apply user sentiment", psdApplyUserSentiment);
  psdSafe("load user sentiment", psdLoadUserSentiment);

  setTimeout(() => psdSafe("apply user sentiment 500", psdApplyUserSentiment), 500);
  setTimeout(() => psdSafe("apply user sentiment 1500", psdApplyUserSentiment), 1500);
  setTimeout(() => psdSafe("apply user sentiment 3000", psdApplyUserSentiment), 3000);
}

if(document.readyState === "loading"){
  document.addEventListener("DOMContentLoaded", psdInit);
}else{
  psdInit();
}

window.addEventListener("load", () => {
  psdSafe("create vote widget on load", psdCreateVoteWidget);
  psdSafe("create PDF widget on load", psdCreatePdfWidget);
  psdSafe("apply compact ribbon layout on load", psdApplyCompactRibbonLayout);
  psdSafe("create advertise banner on load", psdCreateAdvertiseBanner);
  psdSafe("ensure market charts nav link on load", psdEnsureMarketChartsNavLink);
  psdSafe("normalize top nav icons on load", psdNormalizeTopNavIcons);
  psdSafe("enhance footer legal links on load", psdEnhanceFooterLegalLinks);
  psdSafe("enhance social links on load", psdEnhanceSocialLinks);
  psdSafe("apply user sentiment on load", psdApplyUserSentiment);
});

window.psdApplyUserSentiment = psdApplyUserSentiment;

/*
  PSD Live Update
  - Shared auto-update layer for every page that loads vote-widget.js.
  - Adds cache-busting for same-site JSON data files.
  - Checks /status.json every 5 minutes.
  - Reloads once when new published data is detected.
*/
(function psdSiteLiveUpdateBootstrap(){
  const PSD_LIVE_STATUS_PATH = "/status.json";
  const PSD_LIVE_CHECK_MS = 5 * 60 * 1000;
  const PSD_LIVE_FIRST_CHECK_MS = 15000;
  const PSD_LIVE_STORAGE_KEY = "psd_live_status_signature_v1";
  const PSD_LIVE_RELOAD_KEY = "psd_live_reloaded_signature_v1:" + window.location.pathname;
  const PSD_JSON_DATA_FILE = /\/(?:dashboard_data|dashboard_history|sentiment_history|technical_data|status|news_cache|news_articles|latest_news|articles|news)\.json$/i;

  function psdInstallFreshJsonFetchGuard(){
    if(window.PSD_FRESH_JSON_FETCH_GUARD_INSTALLED || typeof window.fetch !== "function") return;
    window.PSD_FRESH_JSON_FETCH_GUARD_INSTALLED = true;

    const originalFetch = window.fetch.bind(window);

    window.fetch = function(input, init){
      try{
        const rawUrl = typeof input === "string"
          ? input
          : (input instanceof URL ? input.toString() : (input && input.url ? input.url : ""));

        if(rawUrl){
          const url = new URL(rawUrl, window.location.href);

          if(url.origin === window.location.origin && PSD_JSON_DATA_FILE.test(url.pathname)){
            url.searchParams.set("_psd_fresh", Date.now().toString());

            const nextInit = Object.assign({}, init || {}, { cache: "no-store" });

            if(typeof input === "string" || input instanceof URL){
              return originalFetch(url.toString(), nextInit);
            }

            if(input && input.url){
              return originalFetch(url.toString(), nextInit);
            }
          }
        }
      }catch(error){
        console.warn("PSD fresh JSON fetch guard skipped:", error);
      }

      return originalFetch(input, init);
    };
  }

  function psdStatusSignatureFromText(text){
    if(!text) return "";

    try{
      const json = JSON.parse(text);
      const direct =
        json.updated ||
        json.updated_at ||
        json.generated_at ||
        json.last_updated ||
        json.timestamp ||
        json.time ||
        json.run_id ||
        json.workflow_run ||
        "";

      if(direct) return String(direct);

      return JSON.stringify(json);
    }catch(error){
      return String(text);
    }
  }

  function psdIsPageSafeToReload(){
    if(document.visibilityState && document.visibilityState !== "visible") return false;

    const votePanel = document.getElementById("psdVotePanel");
    if(votePanel && votePanel.classList.contains("open")) return false;

    const active = document.activeElement;
    if(active && ["INPUT","TEXTAREA","SELECT"].includes(active.tagName)) return false;

    return true;
  }

  async function psdCheckForFreshSiteData(){
    if(window.PSD_LIVE_UPDATE_CHECKING) return;
    if(!psdIsPageSafeToReload()) return;

    window.PSD_LIVE_UPDATE_CHECKING = true;

    try{
      const url = new URL(PSD_LIVE_STATUS_PATH, window.location.origin);
      url.searchParams.set("_psd_status", Date.now().toString());

      const response = await fetch(url.toString(), { cache: "no-store" });
      if(!response.ok) return;

      const text = await response.text();
      const signature = psdStatusSignatureFromText(text);
      if(!signature) return;

      const previousSignature = localStorage.getItem(PSD_LIVE_STORAGE_KEY);
      const alreadyReloadedSignature = sessionStorage.getItem(PSD_LIVE_RELOAD_KEY);

      if(!previousSignature){
        localStorage.setItem(PSD_LIVE_STORAGE_KEY, signature);
        return;
      }

      if(previousSignature !== signature && alreadyReloadedSignature !== signature){
        localStorage.setItem(PSD_LIVE_STORAGE_KEY, signature);
        sessionStorage.setItem(PSD_LIVE_RELOAD_KEY, signature);

        psdTrack("site_auto_refresh", {
          page_path: window.location.pathname,
          status_signature: signature.slice(0, 80)
        });

        setTimeout(() => {
          window.location.reload();
        }, 650);

        return;
      }

      localStorage.setItem(PSD_LIVE_STORAGE_KEY, signature);
    }catch(error){
      console.warn("PSD live update check failed:", error);
    }finally{
      window.PSD_LIVE_UPDATE_CHECKING = false;
    }
  }

  function psdStartLiveUpdate(){
    if(window.PSD_LIVE_UPDATE_STARTED) return;
    window.PSD_LIVE_UPDATE_STARTED = true;

    setTimeout(psdCheckForFreshSiteData, PSD_LIVE_FIRST_CHECK_MS);
    setInterval(psdCheckForFreshSiteData, PSD_LIVE_CHECK_MS);

    document.addEventListener("visibilitychange", () => {
      if(document.visibilityState === "visible"){
        setTimeout(psdCheckForFreshSiteData, 1500);
      }
    });

    window.addEventListener("focus", () => {
      setTimeout(psdCheckForFreshSiteData, 1500);
    });
  }

  psdInstallFreshJsonFetchGuard();

  if(document.readyState === "loading"){
    document.addEventListener("DOMContentLoaded", psdStartLiveUpdate);
  }else{
    psdStartLiveUpdate();
  }

  window.psdCheckForFreshSiteData = psdCheckForFreshSiteData;
})();
